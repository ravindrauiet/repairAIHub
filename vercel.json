{
    "version": 2,
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/index.html" }
    ],
    "buildCommand": "npm install && mkdir -p public && touch public/favicon.ico && mkdir -p public/images && npm run build",
    "outputDirectory": "dist"
  }